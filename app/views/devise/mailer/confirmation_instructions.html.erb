<p><%= t('views.device.greeting')%> <%= @email %> </p>

<p><%=t('views.device.confirmation.text_for_user')%>:</p>

<p><%= link_to t('views.device.confirmation.confirm_account'), confirmation_url(@resource, confirmation_token: @token) %></p>
